# 结构体

## 1、C++灵魂拷问：结构体可以使用memset初始化吗
C++结构体仅在同时满足“仅包含 POD 类型成员”“无虚函数和虚继承”和“未定义自定义构造函数”三个条件时才可以使用memset初始化。

memset(&s, 0, sizeof(Student))会将std::string的内部成员（如指向堆内存的指针、长度计数器）直接清零，破坏其内部状态。
后续执行s.name = "Alice"时，std::string的赋值运算符会尝试释放被清零的指针（已无效），导致未定义行为（UB），通常表现为运行时崩溃（如堆损坏、段错误）。

