// main.cpp
import math_lib;
import <iostream>;  // C++20 也支持对标准库头文件使用 import

int main()
{
    std::cout << add(2, 3) << std::endl;       // 输出 5
    std::cout << multiply(2, 3) << std::endl;  // 输出 6
    return 0;
}