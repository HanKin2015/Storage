# 1、问题
比如我要新增一个vdc，我需要判断当前是否存在此vdc，如果存在则将用户添加进去，不存在则新增，同理用户不在此vdc中，则新增，否则直接修改里面的值。

# 2、总结
第一种格式的优势：

直接通过 IP 地址访问，查找和更新操作简单高效。
适合于需要频繁添加、更新或删除 VDC 和用户信息的场景。
第二种格式的劣势：

需要遍历数组来查找 VDC 和用户，增加了复杂性和性能开销。
在处理动态数据时，操作相对繁琐。
因此，虽然第二种格式在某些情况下也可以使用，但在灵活性和操作简便性方面，第一种格式通常更具优势。