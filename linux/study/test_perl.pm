#!/usr/bin/perl

print "Hello, World!\n";

my $IS_XHCI = 0;
my $id = '123';
my $age = 12;

if ( $id eq '12' or $id eq '123' ) {
	print "id equal\n";
} else {
	print "id not equal\n";
}

if ( $age eq 1 or $age eq 12 ) {
	print "age equal\n";
} else {
	print "age not equal\n";
}

